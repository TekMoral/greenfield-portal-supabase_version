import React from 'react';

/**
 * CloudinaryUpload (Deprecated)
 * Cloudinary has been removed from this app. This component is a placeholder to prevent usage.
 * Replace usages with Supabase Storage-based upload flows.
 */
const CloudinaryUpload = () => {
  return (
    <div className="p-4 border border-yellow-400 bg-yellow-50 text-yellow-800 rounded">
      <p className="font-semibold">Cloudinary is no longer used.</p>
      <p>Replace this component with a Supabase Storage upload component or flow.</p>
    </div>
  );
};

export default CloudinaryUpload;
